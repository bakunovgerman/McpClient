{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceFolder}"
      ]
    },
    "commands": {
      "command": "npx",
      "args": ["-y", "mcp-server-commands"]
    }
  }
}
